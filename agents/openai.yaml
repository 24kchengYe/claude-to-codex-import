interface:
  display_name: "Claude to Codex Import"
  short_description: "Migrate Claude Code sessions into Codex"
  default_prompt: "Use $claude-to-codex-import to import my local Claude Code conversations into Codex."
policy:
  allow_implicit_invocation: true
